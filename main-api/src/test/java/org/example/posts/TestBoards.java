package org.example.posts;

import org.example.boards.domain.entity.Board;
import org.example.boards.domain.entity.Title;

public class TestBoards {
    public static final Board board = new Board(new Title("title"));
}
